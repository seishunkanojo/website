---
import Layout from "../../layouts/Layout.astro";
import Heading from "../../components/elements/Heading.astro";
import CastCard from "../../components/blocks/CastCard.astro";
import Button from "../../components/elements/Button.astro";
import { fetchCasts } from "../../lib/fetchCasts";
const {casts} = await fetchCasts();
---

<Layout title="彼女一覧 | 青春彼女_都内No.1の彼女レンタルサービス" description="東京であなたにぴったりのレンタル彼女を見つけませんか？当サービスでは、様々なタイプのキャストが在籍。写真やプロフィールから、理想のデート相手を探せます。あなたの特別な時間を、素敵なキャストと一緒にお過ごしください。キャストも募集中です！">
    <div class="pt-20 md:pt-24 container mx-auto px-4 sm:px-6 lg:px-8">
        <section class="py-12">
            <!-- ヒーローセクション -->
            <div class="mb-16 text-center">
                <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold font-klee text-gray-800 mb-4">
                    <span class="text-primary-600">理想の彼女</span>を見つける
                </h1>
                <p class="text-gray-600 max-w-2xl mx-auto text-sm md:text-base lg:text-lg">
                    あなたにぴったりの彼女を見つけて、特別な時間を過ごしませんか？
                    様々なタイプのキャストが在籍しています。
                </p>
                <div class="w-24 h-1 bg-gradient-to-r from-primary-400 to-accent-400 mx-auto mt-6 rounded-full"></div>
            </div>

            <!-- キャスト一覧 -->
            <Heading heading="Cast List" subtitle="彼女一覧"></Heading>
        
            <!-- grid表示。一番画面幅が小さいときは2列。一番広いときは4列。 -->
            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 lg:gap-8 max-w-[1400px] mx-auto mb-16">
                {casts.map((cast, index) => (
                    <a href={`/cast/${cast.slug}`} class="animate-fade-in" style={`animation-delay: ${index * 0.1}s`}>
                        <CastCard
                            name={cast.fullName}
                            age={cast.age}
                            price={cast.price}
                            profileImage={cast.thumbnail.src}
                            tagList={cast.tagList.map(item => item.tag)}
                        />
                    </a>
                ))}
            </div>

            <!-- 求人募集バナー -->
            <div class="bg-gradient-to-r from-primary-50 to-accent-50 p-8 rounded-2xl shadow-soft mb-16">
                <div class="flex flex-col md:flex-row items-center justify-between gap-6">
                    <div>
                        <h2 class="text-xl md:text-2xl font-bold text-primary-700 mb-2 font-klee">キャストとして働いてみませんか？</h2>
                        <p class="text-gray-600 mb-4">研修時から<span class="font-bold text-primary-600">時給3000円</span>保証！あなたの魅力を活かして働きませんか？</p>
                    </div>
                    <a 
                        href="/careers" 
                        class="inline-block bg-gradient-to-r from-primary-500 to-accent-500 text-white px-6 py-3 rounded-full 
                            shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-all duration-300 
                            font-medium text-sm md:text-base"
                    >
                        求人情報を見る <i class="fa-solid fa-arrow-right ml-2"></i>
                    </a>
                </div>
            </div>

            <div class="flex justify-center">
                <a 
                    href="/" 
                    class="inline-block bg-white text-primary-600 border border-primary-300 px-6 py-3 rounded-full 
                        shadow-sm hover:shadow-md hover:bg-primary-50 transform hover:-translate-y-0.5 transition-all duration-300 
                        font-medium text-sm md:text-base"
                >
                    <i class="fa-solid fa-home mr-2"></i> トップページへ戻る
                </a>
            </div>
        </section>
    </div>
</Layout>